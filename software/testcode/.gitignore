# -------------------------------
# 1. 编译输出文件 (必须忽略)
# -------------------------------
# 这些文件由编译器生成，可以从源码重新创建，提交它们会导致仓库体积膨胀和合并冲突
*.o          # 目标文件
*.obj        # 目标文件
*.d          # 依赖文件
*.crf        # 交叉引用文件
*.htm        # 列表文件 (HTML格式)
*.lst        # 列表文件
*.map        # 链接映射文件
*.axf        # ARM可执行文件（调试信息）
*.hex        # Intel HEX格式文件（可烧录文件）
*.bin        # 二进制文件
*.elf        # ELF格式文件
*.sct        # 分散加载文件（若为自动生成则应忽略）
*.lnp        # 链接器输出文件
*.dep        # 依赖关系文件
*.iex        # 中间执行文件

# -------------------------------
# 2. 用户环境配置 (必须忽略)
# -------------------------------
# 这些文件记录了你的个人调试设置和IDE布局，提交会导致团队成员的配置相互覆盖
*.uvoptx     # 用户工程选项文件（调试器设置、断点等）
*.uvguix.*   # IDE窗口布局配置
*.uvgui.*    # 旧版IDE窗口布局文件

# -------------------------------
# 3. 编译器/IDE临时文件 (建议忽略)
# -------------------------------
*.bak        # 备份文件
*.tmp        # 临时文件
*.log        # 日志文件
JLinkLog.txt # J-Link调试器日志
*.dbgconf    # 调试配置文件

# -------------------------------
# 4. 默认输出文件夹 (强烈建议忽略整个目录)
# -------------------------------
# 很多MDK工程会将所有中间文件统一放在这两个文件夹中
Objects/
Listings/
RTE/         # Run-Time Environment 生成的文件
Templates/
Examples/